
# Temperature Recording Client Configuration Data:

# Connection to Broker
broker_ip:   192.168.2.32
broker_port: 1883

# Recording Client configuration
client_name: rec0               # Client name: REcordingClient 0
outfile_name: tms_data.json     # Default name of datafile, if not specified on the cmd-line 
logfile_name: tms_data.log      # Default name of log-file, if not specified on the cmd_line

# Timestamp mode
ts_mode: system_time            # system_time: usually the local time and date
                                # zero_time_01: time starts at 00:00, 01.01.2000
                                # zero_time_02: time starts at 000:00, no date, only hours, wrap-around after 999:59 (about 49 days)

#